# islands 要件定義書

> `C:/Users/zk-ht/Downloads/islands/islands/.agents/rules/rules.md` の作法に従い、
> コードを変更したらこのファイルにも反映すること。

## 1. 概要

islands は、自分と似た活動をしている人を簡単に見つけたり、界隈内で交流したりする
ための Web アプリケーション。投稿は「島」という形でマップ上にクラスタリングされ、
似たジャンルの投稿が自動的に集まる。

**元の islands との違いは、座標が本物であること。** 元のプロトタイプは
`Math.random()` で投稿を配置し、島の名前を12個の固定キーワード表から多数決で
決めていた。本実装では、投稿された文章そのものを448次元に埋め込み、凍結した
エンコーダで2次元に落として座標を決める。したがって

- **近い＝似た話をしている** が文字通り成立し、
- 島の名前も、その島に立っている投稿の語から生成される。

固定ジャンルは存在しない。マップは名前のない海から始まり、人が来るたびに
その人たちの言葉で名前が増えていく。

---

## 2. アカウント

### 2.1 認証
- Supabase Auth によるメールワンタイムパスコード（6桁）
- 登録とログインは同一フロー。初見のアドレスなら自動でアカウントが作られる
- Google / Apple のソーシャルログインは、環境変数で有効化されている場合のみ表示
- **モックではない。** 任意の4桁で通ることはない

### 2.2 プロフィール（`accounts`）
| 項目 | 制約 |
|---|---|
| 名前 | 必須・16字まで |
| 所属組織 | 任意・32字まで |
| 自己紹介 | 任意・300字まで |
| リンク | 任意・200字まで。表示時に http(s) のみ許可 |
| アイコン | 30種の組み込みアバター、または画像アップロード |

メールアドレスは `auth.users` にのみ保存し、`accounts` には持たない
（このテーブルは全員が読めるため）。

---

## 3. 投稿機能

### 3.1 投稿内容（`posts`）
- **本文**: 30〜140字。**これだけが座標を決める唯一の入力**
  - 140字は islands の上限。30字は埋め込みが意味を持つための下限
  - URL とメールアドレスは保存前に `[リンク]` `[メール]` に置換する
- **画像添付**: 任意（1枚）。ブラウザ側で長辺1280px・WebP・500KB以下に縮小してから
  Supabase Storage に直接アップロード
- **タグ選択**（任意・複数選択可能なチェックボックス）:
  - 気軽に話しかけて
  - 助けてほしい
  - 参加者募集中
  - 仲間募集中
- **モチベーションスライダー**: エンジョイ（0）〜ガチ（100）

### 3.2 編集・削除
- 本文の編集は**投稿を地図上で移動させる**。編集画面にその旨を明示する
- タグ・熱量・画像の変更は座標を動かさない
- 削除は論理削除。他人が書いたコメントは残る

---

## 4. マップ

### 4.1 座標
```
本文
  ├─ どの語を使ったか  Sudachi で分かち書き → 単語TF-IDF(1-2gram) ⊕ 文字TF-IDF(2-4gram)×0.25
  │                     → TruncatedSVD(64) → L2                              …… 64次元
  └─ どんな意味か      multilingual-e5-small (ONNX Runtime) で
                        「生の文」×0.35 + 「内容語だけの文」×0.65 → L2         …… 384次元

  → hstack([意味×0.65, 語×0.35]) → L2                                        …… 448次元
  → 凍結エンコーダ f (448→512→256→128→2, GELU+LayerNorm)
  → 等方スケール → 0-1000 の座標
```
- 同じ文章は毎回**小数点まで完全に同じ座標**になる
- 誰が新しく投稿しても**既存の投稿は1pxも動かない**

### 4.2 エネルギー計算
- **計算式**: `motivation + (likeCount + helpCount + joinCount + commentsCount) * 5`
- 元の islands は `likes + comments` のみだったが、リアクションが3種になったため
  すべて数える。「手伝えるかも」は「いいね」より弱い信号ではない
- Postgres の生成列。カウントと食い違うことがない

### 4.3 プロットアイコン（インタラクション数に応じた段階的アイコン）
| インタラクション数 | アイコン | 説明 |
|---|---|---|
| 0〜4 | 🪵 | 通常 |
| 5〜9 | 🛖 | 盛り上がり始め |
| 10〜29 | 🏠 | 注目 |
| 30以上 | 🏰 | 話題沸騰 |

低ズームではこのアイコン、高ズームでは投稿者のアバターを描画する。

### 4.4 バイオーム（地形）描画
各投稿が半径 `(30 + energy * 0.45) * (2/3)` の円錐（中心が最大、縁でゼロの線形減衰）
を作り、**加算合成**する。合計値で地形が決まる:

| バイオーム | 合計エネルギー | 色 |
|---|---|---|
| 海 | < 0.05 | #2FA6FF |
| 浅瀬 | >= 0.05 | #4BB9FF |
| 砂漠 | >= 30 | #E6CF9B |
| サバンナ | >= 50 | #C8B676 |
| 草原 | >= 90 | #81C784 |
| 森 | >= 300 | #41A873 |
| 山 | >= 700 | #34806C |

**一つの投稿のエネルギーではなく、その地点の合計値**で決まる。静かな投稿が2つ
隣り合うと、単独では砂漠にしかならない場所が草原になる。これが仕組みの核心。

### 4.5 島の検出（クラスタリング）
- Union-Find アルゴリズムによる投稿のクラスタリング
- 各投稿のエネルギー影響半径が重なる投稿を同じ島とみなす
- 推移的に閉じる。A-B と B-C が重なれば A-C が離れていても一つの大陸
- 50件以上で「大陸」、未満で「島」

### 4.6 島の名前
- **ジャンル投票は行わない。** その島にいる投稿の名詞を集め、
  「何人が使ったか × コーパスでの珍しさ(idf)」で上位2語を選ぶ
- 名前が付けられない島には名前が無く、地図にも出ない
- 同じ名前を2つの島が持つことはない（大きい島が優先）
- 色は凍結 k-means 領域（`cluster_id`）が決める。島がくっついても色は動かない

### 4.7 マップ画面のUI
- 検索フォーム（本文・投稿者名の部分一致）
- タグフィルタリング（OR検索）
- 「全体マップ」/「まわりの人」の切り替え
- 現在地ボタン（自分の島にカメラを戻す）
- 自分がいる島の名前を表示するバッジ

### 4.8 まわりの人（orbit）
- 自分を中心に、他の投稿を **似てる度** の半径・**実際の方角**の角度で配置
- 似てる度は地図の距離ではなく、潰す前の448次元コサインから計算する
  （このビルドでは本来の近傍の34%しか2次元投影に残らないため）

---

## 5. 投稿カード表示

### 5.1 表示要素
- ユーザー情報（アイコン・名前・経過時間）
- 自分の投稿の場合のみ、右上にメニュー（編集・削除）アイコン
- 他人の投稿の場合、**似てる度（%）と共有していることば**
- タグ（バッジ形式で複数表示）
- 本文
- モチベーションスライダー（エンジョイ〜ガチ）
- 画像
- コメント数・いいね数・手伝えるかも数・参加したい数

### 5.2 リアクション
| 種類 | 意味 |
|---|---|
| いいね | 反応した |
| 手伝えるかも | 力を貸せる |
| 参加したい | 一緒にやりたい |

同じ人が同じ投稿に同じ種類を2回押しても1回として扱う（複合主キーで保証）。

### 5.3 メッセージ（コメント）
- 投稿ごとのスレッド。500字まで
- シートを全画面まで開いたときのみ表示し、入力欄は下部固定
- 開いている間だけ5秒間隔でポーリング（WebSocket は使わない）
- 各メッセージに操作がひとつ。自分の発言なら**削除**（論理削除。件数は
  トリガが戻す）、他人の発言なら**報告**

---

## 6. 通知
- **サーバー側で生成する**（DBトリガ）。端末をまたいで既読が一致する
- 自分の操作は自分に通知されない
- 種類: いいね / 手伝えるかも / 参加したい / メッセージ
- 未読バッジをボトムナビに表示
- タップすると該当投稿をマップ上で開く
- 未読の通知は、リアクションを取り消すと撤回される。既読のものは残る

---

## 7. 画面構成
| 画面 | ルート |
|---|---|
| イントロ（3枚） | `#/intro` |
| ログイン / 登録 | `#/auth` |
| プロフィール作成 | `#/setup` |
| マップ | `#/map` |
| 新規投稿 | `#/post` |
| 投稿の編集 | `#/post/:id/edit` |
| 通知 | `#/notifications` |
| マイページ | `#/me` |
| プロフィール編集 | `#/me/edit` |
| 他人のプロフィール | `#/u/:id` |

ボトムナビゲーション: マップ / 投稿 / 通知 / マイページ

未ログインでもマップの閲覧と投稿の表示はできる。投稿・リアクション・
コメント・通知にはアカウントが要る。

---

## 8. 技術スタック
| | |
|---|---|
| サーバー | FastAPI（Python 3.12）。torch を一切読み込まない |
| 埋め込み | multilingual-e5-small を ONNX Runtime (fp32) |
| 投影 | 凍結エンコーダの numpy 実装 |
| DB / 認証 / ストレージ | Supabase（PostgREST + GoTrue + Storage + pgvector） |
| フロントエンド | 素の ES モジュール。ビルド不要・実行時の外部依存ゼロ |
| 地図描画 | Canvas 2D（PixiJS は使わない） |
| 配信 | Google Cloud Run（1コンテナ） |

### 8.1 データ経路
```
ブラウザ ──(anon key + ユーザー JWT)──> Supabase   … 読み取り・コメント・
   │                                                リアクション・通知・画像
   └──(ユーザー JWT)──> Cloud Run (FastAPI)      … 埋め込みが要る書き込みだけ
```
`service_role` キーは Cloud Run だけが持つ。ブラウザが持つ anon キーは公開前提の
値で、守っているのは `supabase/schema.sql` の RLS ポリシー。

---

## 9. 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-07-27 | タグ選択をラジオボタンからチェックボックス4種に変更 |
| 2026-07-27 | マップ描画のエネルギー計算を時制ベースからリアクション・コメント数ベースに変更 |
| 2026-07-27 | プロットアイコンを時制ベースからリアクション数に応じた段階的アイコンに変更 |
| 2026-07-28 | 自分の投稿カードにメニューアイコン（編集・削除）を追加 |
| 2026-07-28 | プロフィール編集画面を追加 |
| 2026-07-30 | 投稿カードに「手伝えるかも」「参加したい」を追加 |
| 2026-07-30 | 島の描画をグラデーションなしの単色塗り分けに変更 |
| 2026-07-31 | 砂漠より低いエネルギー帯として「浅瀬」バイオームを追加 |
| 2026-07-31 | アプリ内通知機能を動的化 |
| 2026-08-20 | 投稿の「タイトル（取り組み）」を削除し、本文のみに統合 |
| 2026-08-27 | **座標を乱数から意味ベースに変更。** 本文を448次元に埋め込み、凍結エンコーダで2次元に投影する方式に置き換え。同じ文章は常に同じ座標に着き、新しい投稿が既存の投稿を動かさない |
| 2026-08-27 | **島の命名から固定ジャンル表（GENRE_MAP、12種）を削除。** 島にいる投稿の名詞を「使った人数 × コーパスでの珍しさ」で並べて命名する方式に変更。名前を付けられない島は地図に名前を出さない |
| 2026-08-27 | 投稿カードに「似てる度（%）」と「共有していることば」を追加。448次元コサインから計算し、地図上の距離は使わない |
| 2026-08-27 | 「まわりの人」ビューを追加。似てる度を半径、実際の方角を角度として配置する |
| 2026-08-27 | **アカウントを Supabase Auth の実装に変更。** 「任意の4桁で通る」モック OTP を廃止 |
| 2026-08-27 | 本文の文字数下限を30字に設定（埋め込みが意味を持つ下限）。上限は140字のまま |
| 2026-08-27 | エネルギー計算に「手伝えるかも」「参加したい」を追加。Postgres の生成列に変更 |
| 2026-08-27 | 通知をクライアント生成からDBトリガ生成に変更。複数端末で既読が一致するようになった |
| 2026-08-27 | 他人のプロフィール画面が `accounts` の実データを読むように変更（従来は「まだ登録されていません」固定） |
| 2026-08-27 | マイページのタブ件数のハードコード（メッセージ345 / 協働1）を実際の集計に変更 |
| 2026-08-27 | 「投稿を編集」の未実装アラートを実装に置き換え。本文を変更すると座標が動くことを画面に明示 |
| 2026-08-27 | マップの現在地ボタンが実際にカメラを移動するように変更（従来は resize イベントを投げるだけ） |
| 2026-08-27 | Google / Apple ログインボタンを、プロバイダが設定されている場合のみ表示するよう変更 |
| 2026-08-27 | 画像添付を Supabase Storage に保存するよう実装。ブラウザ側で1280px・WebP・500KB以下に縮小 |
| 2026-08-27 | 通報機能（`reports`）を追加 |
| 2026-08-27 | 低ズームでは段階アイコン、高ズームではアバターを描画するよう変更 |
| 2026-08-27 | 投稿のレート制限を IP 単位からアカウント単位に変更（会場の Wi-Fi で後続が弾かれる問題を解消） |
| 2026-08-27 | 定員（MAX_USERS=200）を撤廃。アカウント制がその役割を担う |
| 2026-08-27 | `[hidden]` の CSS 規則が無く、`display` を自前で持つ要素（ボトムナビ・タグ絞り込みパネル・島バッジ・未読バッジ・パスコード枠・一部ボタン）が隠したつもりで表示され続けていたのを修正 |
| 2026-08-27 | プロフィールの任意項目（所属組織・自己紹介・リンク・アイコン画像）を空に戻せるようにした。API が明示的な null を「消す」として扱う |
| 2026-08-27 | アイコンの画像アップロードを実装（要件2.2の後半）。ブラウザ側で256px・WebP・120KB以下に縮小し、保存時にアップロードする |
| 2026-08-27 | メッセージに「削除」（自分の発言）と「報告」（他人の発言）を追加。DB側の論理削除・件数戻し・`reports.comment_id` は既にあり、UIだけが無かった |
| 2026-08-27 | シートを開いている投稿を地図上で示すようにした。低ズームでは 📍（islands の挙動）、高ズームでは島色のリング |
