# islands デザイン移植・実装監査

更新日: 2026-09-09。最新の差分解消結果は次節。以降の2026-09-08の未確認記述は当時の監査記録。

## 2026-09-09 参照画面との差分解消

- `Downloads/islands/islands` の22ソースhashが固定記録と一致。localhost:3001のプロセスが `next dev --webpack` の子であることを確認した。
- 375/390/768/1440 × 高さ900pxで参照と改修版の入口・登録・ガイド3画面・初回投稿・通常投稿・地図・プロフィール・編集・通知を撮影。`output/reference-*.png` と `output/candidate-*.png` 各44枚。動的投稿データは異なるため、地形や実件数のピクセル一致とは扱わない。
- 入口を参照の登録選択画面に統一。前回の登録前introは初期導線から外した。登録フォームは名前・所属・メール→実OTP認証→プロフィール保存→ガイド。
- ガイドはSUCCESS→SUCCESS_2→GUIDANCE_1の文章・順序・左寄せ・改行・配置を移植。初回投稿はGUIDANCE_2の専用見出しと下部ボタン、本文→熱量→画像→タグの順。成功時は実際の投稿を地図に表示。
- ガイドの途中位置をアカウント別に保存し、起動し直しても再開。初回投稿成功で完了。ブラウザ保存不可時はメモリ内で継続する。
- 登録・投稿の余白とフォント、所属と氏名の横並び、タブ間隔を調整。登録保存失敗時は入力内容をプロフィール設定へ引き継ぐ。
- ローカル回帰16件成功（4幅、実ローカルOTP、プロフィール保存、ガイド再起動、初回投稿、地図パネル操作）。同じシナリオの手動CLI実行でも4幅で登録→投稿→地図に成功。
- 意図的な差分: 参照の任意4桁認証・ランダム配置・固定件数・未接続OAuthボタンは採用しない。実Supabase認証、Gemini配置、実件数、設定済OAuthだけを維持。本文30文字以上の既存契約は文字数欄に表示。ガイドの戻る操作、画像保存、アクセシブルなラベルを保持。
- 実機キーボード・同一データを使った地形の厳密比較は未確認。公開反映結果は運用手順に記録する。

## 比較基準

正本は `C:\Users\zk-ht\Downloads\islands\islands` の `src/`。
ユーザー指定の起動方法は、そのディレクトリで `npx next dev --webpack`。
PNG、同梱デザイン画像、以前の production build を比較基準にしない。
参照ファイルのSHA-256は [reference_source_manifest.json](reference_source_manifest.json) に保存。
参照側のコードは変更していない。

既存の参照開発サーバーでは `/map` のDOMを表示できたが、起動プロセスの
`--webpack` 指定を未確認。比較用の4幅スクリーンショットは未取得。
したがって「外観一致を実機確認済み」とは扱わない。

## 画面ごとの差分と実装先

参照パスはすべて正本ディレクトリに対する相対パス。

| 対象 | 参照ソース | 差分の採用内容 | 実装先 / 残る確認 |
|---|---|---|---|
| 共通 | `src/app/globals.css`, `src/components/ui/{Input,Button}.tsx` | 最大512px、左右24px、黒い主要ボタン、青・白・グレー、入力の角丸 | `web/style.css`。4幅の文字折返し・余白は未検証 |
| ナビ | `src/components/common/BottomNav.tsx` | 64px、28px線画、視覚上はアイコンのみ。compose/editでも表示 | `icons.js`, `router.js`。読み上げ名とaria-currentを保持 |
| PC投稿パネル | `src/components/domain/MapApp.tsx` | 768px以上は左上16px、400px、最大80vh、非モーダル、チャットを初期表示 | `screens/map.js`, `ui.js`, CSS。背後の地図・ナビ操作はブラウザ検証待ち |
| モバイルパネル | 同上 | 50/90dvh、入力を下端に配置、Escapeとフォーカス管理 | visualViewport対応を追加。実機キーボード、横画面、safe areaは未検証 |
| 投稿 | `src/app/post/page.tsx` | 本文→文字数→スライダー→画像→タグ。リングを隠し30～140字を簡潔に案内 | `screens/compose.js`。画面往復の下書き、送信再試行キーを保持 |
| 投稿カード | `src/components/domain/PostCard.tsx` | 著者→タグ→本文→熱量→実画像→反応。casual/hardcore維持 | `components/postcard.js`。類似度は後段の開閉式補足、画像全体へのリンク |
| 地図 | `src/components/domain/MapApp.tsx` | 単色バイオームと拠点表示。顔・船・鳥などの地図装飾を抑制 | `map/index.js`, `map/terrain.js`。意味座標はサーバー由来のまま |
| プロフィール | `src/app/profile/page.tsx`, `[userId]/page.tsx`, `edit/page.tsx` | 3タブ、実件数、共通リアクション、本人メール読み取り表示 | `screens/profile.js`。協働は反応の集計と説明。プロフィール編集の下書き保持は未実装 |
| 入口・認証 | `src/app/page.tsx`, `register/page.tsx`, `login/page.tsx` | islands入口、登録/ログインへの入口を分離、設定済OAuthのみ | `index.html`, `screens/auth.js`。OTP処理は既存共通フローを維持。参照の登録案内の完全移植は未完了 |
| 通知 | `src/app/notifications/page.tsx` | 線画と反応別の色、未読の左罫線 | `screens/notifications.js`, CSS。既読・投稿遷移は実データ |

## 維持する仕様

- FastAPI / Supabase / Canvas、投稿ID、実際の認証・画像保存・コメント・反応。
- 日本語とブランド `islands`。タグは現行4種、OR条件。
- 全体マップ、まわりの人、意味の近さと共通語。
- 添付画像がない投稿には仮画像を出さない。
- 時制追加、協働承認ワークフロー、DM、Next.jsへの全面移行は今回の対象外。
- メールアドレス表示から認証メールの変更はできない。

## 操作・地形の修正

| 優先度 | 問題 | 変更 | 検証状態 |
|---|---|---|---|
| P0 | 遷移中の次の遷移が捨てられる | 最新の遷移をキューに保持、準備完了Promiseを返す。固定時間後の投稿表示を廃止 | Node回帰2件成功 |
| P0 | パネル再表示でexpandリスナーが蓄積 | 選択単位のAbortControllerとチャットdestroy | ブラウザ20回開閉テストを追加、未実行 |
| P0 | Aの遅い応答がBを上書き | 選択世代と地図取得世代で古い応答を破棄 | ブラウザ用の遅延応答テスト追加、未実行 |
| P0 | カードの反応状態が画面間で食い違う | 共有状態の1購読、取消と失敗時復元、プロフィールも同じ処理 | 状態更新Node3件成功。同時操作とコメント購読は未検証 |
| P1 | プロフィール往復で選択が消える | 選択投稿を記録し、map再入場で復元。カメラは再フィットしない | 戻る/進む含めブラウザ確認待ち |
| P1 | 初期画角が実投稿と無関係 | 全実投稿の半径込み境界をAPIで取得、単独投稿は倍率上限1.6 | 0/1/6/800/801件の画面確認は未実施 |
| P1 | 最初の260以内の島を所属と推測 | `/api/islands` の `post_ids` で所属を参照 | 5,000件超の命名API上限は未解消 |
| P1 | 絞込みが地形・選択と不一致 | 表示マーカー、クリック対象、地形、島名に同じフィルター | 大量投稿時の未読込マーカーと全対象地形の表示説明は要再評価 |
| P1 | 個別投稿+集計セルが二重加算 | 全対象投稿のサーバーエネルギー格子で地形を置換 | 単体テスト追加、Python未実行 |
| P1 | セル内容変更がキャッシュに反映されない | 値・範囲・解像度に由来するrevision、フィルター変更時の失効 | 大量投稿の負荷と境界継目は未検証 |

## 比較画像と受け入れ検証

`tests/browser/regression.spec.mjs` は375/390/768/1440pxで入口、未認証ガード、
下書き往復、パネル開閉、非モーダル/モーダルを確認する。出力先は
`output/browser-tests/` と `output/playwright-report/`。現時点で画像は存在しない。

正式比較では参照と改修版に同じ6投稿・画像・反応・コメントを与え、登録、投稿、
map、半展開/全展開、通知、プロフィールを同じ幅・高さ・選択状態で撮影する。
参照のランダム座標だけは比較用の固定データで揃え、本番の意味座標は変更しない。
参照側の固定件数と未接続ボタンは見た目の根拠に限定し、成功動作の期待値にしない。

未完了: 4幅の正式比較、実機キーボード、OTP失敗/再送、画像保存/失敗、
選択競合の遅延テスト、20回開閉時の実リスナー/通信数の計測、
0/1/6/800/801件、ブラウザ戻る/進む、削除済み投稿、通信失敗からの復帰。

## リリース状態

コード追加中・検証未完了。本番デザイン改修、Gemini成果物再構築、既存投稿の再配置、
本番切替は未実施。外観改修のE5リリースを先に検証し、Geminiを別リリースとする。
